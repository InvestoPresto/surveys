<fieldset class="question">
  <%= f.label :content, "Question" %><br />
  <%= f.text_area :content, :rows => 3 %><br />
  <%= f.link_to_remove "Remove Question" %>
  <div class="choices">
    <%= f.fields_for :choices %>
  </div>
  <%= f.link_to_add "Add a choice", :choices %>
</fieldset>
